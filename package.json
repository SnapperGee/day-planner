{
  "name": "day-planner",
  "version": "0.0.1",
  "description": "Daily event/reminder schedule manager.",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/SnapperGee/day-planner.git"
  },
  "scripts": {
    "build": "cp -r ./src/main/css ./build & cp src/main/html/index.html ./build && webpack",
    "prebuild": "rm -rf build && mkdir ./build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts": "tsc -p ./tsconfig.json"
  },
  "keywords": [
    "schedule",
    "planner",
    "productivity",
    "agenda"
  ],
  "author": "Graham Auger",
  "license": "ISC",
  "devDependencies": {
    "@types/jquery": "^3.5.17",
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "dayjs": "^1.11.9",
    "jquery": "^3.7.1"
  }
}
